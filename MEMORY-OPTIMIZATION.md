# 内存优化说明

## “M Notes 网页内容”进程

macOS 编辑器使用 WKWebView。“网页内容”是 WebKit 的独立渲染进程，不是本地 HTTP 服务。WKWebView 没有受支持的配置将它合并到应用主进程；本次继续保留现有编辑器功能和进程架构。

参考：https://docs.webkit.org/Deep%20Dive/Architecture/WebKit2.html

## 本次改动

- 切换语言保留同一个 WKWebView，通过现有 JS 接口更新样式与文案，避免重复加载整个编辑器和运行时。
- SwiftUI 移除编辑器时取消通知监听、移除脚本消息处理器、断开代理、停止加载并清空协调器保存的文档内容。正式构建不启用开发者工具。
- Mermaid 改为首次预览或导出图表时加载。同一时刻的请求复用一个加载 Promise，失败后可重试；普通文本、数学公式、主题切换和不含图表的导出不加载它。
- 删除未使用的 KaTeX auto-render 脚本加载，现有公式插件继续直接使用 KaTeX。
- 切换到不同文档时清空旧文档撤销历史；文档加载本身不记入撤销记录。当前文档的正常撤销/重做保留，避免旧文件内容不断累积在历史中。

Mermaid 在首次使用后会留在当前编辑器运行时中。打开包含图表的欢迎文档时仍会加载它，因此节省程度取决于文档内容。

## 验证与测量

`./scripts/test-mnotes.sh` 包含浏览器编辑交互、图表/公式、原生资源加载、HTML/PDF 导出、应用状态以及 WebView 生命周期测试。

`node editor-bundle/tests/memory.cjs <优化前的 editor.html 路径>` 可比较相同短 Markdown 文档的 Chromium JS 堆内存，使用三个全新浏览器实例、显式 GC 后取中位数。本次结果：

| 项目 | 优化前 | 优化后 |
| --- | ---: | ---: |
| JSHeapUsedSize 中位数 | 10,979,136 bytes | 5,114,600 bytes |

这是 Chromium 的 JavaScript 堆测量，约减少 53%，**不是 macOS 的 WKWebView 总内存或活动监视器内存测量**。没有据此承诺应用总内存下降比例。

## 后台节能与休眠

专注模式已移除（macOS 菜单、快捷键、Windows/Linux 工具栏及共享编辑器实现）。

- 进入后台立即暂停动画、光标闪烁、非必要图表预览和延迟统计；Windows/Linux 停止每 250ms 的全文轮询，并立即保存恢复草稿。必要的自动保存和进行中的导出继续完成。
- macOS 暂停文件事件监听，回到前台后重新检查当前文件；未保存内容不会被外部文件覆盖，TXT 按原编码读取。
- 连续后台 5 分钟后休眠：macOS 压缩文档、选择、滚动位置和撤销历史快照，再移除 WKWebView；Windows/Linux 卸载编辑器 iframe。正在导出或有未完成的输入/图片导入时延后休眠。
- 返回前台重建编辑器并恢复会话。外部修改导致文档内容不同或切换到其他文件时，不恢复过时快照。
- 快照获取失败时保留原编辑器；休眠不强制保存关闭自动保存的文档，也不清除用户撤销历史。

WebKit 系统进程的退出时机由 macOS 决定，卸载 WebView 不等同于强行终止系统进程。此机制减少编辑器的后台工作和长期驻留资源，不承诺固定内存数值。
