{
  "name": "m-notes-linux",
  "version": "1.0.6",
  "description": "M Notes for Linux - Modern WYSIWYG Markdown & Plain-Text Editor",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "prepare:ui": "node ../scripts/prepare-desktop.cjs",
    "test:ui": "npm run prepare:ui && node ../tests/DesktopUISmoke.cjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "esbuild": "^0.25.0",
    "playwright": "^1.63.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "@tauri-apps/plugin-fs": "^2.0.0"
  }
}
