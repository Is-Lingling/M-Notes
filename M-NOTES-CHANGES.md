# M Notes — 本轮改动

1. 选区与符号折叠：Markdown 标记（标题 #、粗体、斜体、删除线、行内代码、引用、链接等）在非光标行完全隐藏且不占用空间；光标移入该行时自动显示所有标记并正常占用位置，行内支持原生文字选区与编辑。
2. 块级操作：点击代码块标题栏或表格工具栏空白处，选中整块；Delete/Backspace 删除，⌘Z 撤销。
3. 最近使用：标记当前打开文档；缺失文件保留记录，点击后询问移除；按文档保存阅读位置。
4. 工具栏：长标题截断、H 菜单去掉重复箭头，插入图片打开系统文件选择器。
5. 侧边栏：移除文件夹 UI；全文搜索在侧边栏执行，范围为最近使用和当前文档（含尚未保存的内容），⌘⇧F 打开。
6. TXT：纯文本渲染；识别 UTF-8/UTF-16 等系统可检测编码，保存保留读取编码，另存为 UTF-8；支持系统打开文档事件。
7. 导出：HTML 嵌入图片、公式字体和高亮样式，导出前渲染 Mermaid。PDF 等待资源加载，再按文本行、图片和表格行的边界分页，使用矢量 PDF 输出，不依赖打印服务。
8. 品牌：显示名称与产品名称为 M Notes，保留原 bundle identifier 和用户记录，内置 AppIcon 图标。

## 验证

运行 `scripts/test-mnotes.sh`：WebKit 交互与选区、原生 WKWebView 加载、阅读位置、TXT 编码、文件切换、导出 HTML 和分页 PDF。

Xcode 构建：`xcodebuild -project MarkdownNotes.xcodeproj -scheme MarkdownNotes -configuration Debug -derivedDataPath /tmp/mnotes-build build CODE_SIGNING_ALLOWED=NO`。

原生菜单与系统图片选择器的端到端界面复核尚未完成：当前 Mac 锁屏，计算机控制工具无法访问。对应实现已通过编译，底层插入与导出逻辑已测试。
# M Notes build 3

- 移除了标题样式按钮的系统下拉箭头，并限制顶栏文件名和格式按钮组的尺寸，长文件名会在背景块内截断。
- PDF 输出新增单页与 A4 分页两种模式；本地相对路径、百分号编码路径和绝对路径图片都会嵌入输出。
- 左侧栏底部新增偏好设置入口，支持简体中文和英语并持久保存；编辑器内代码复制、图片提示和表格操作也会切换语言。
- 重写内置《M Notes 使用说明》与英文 Guide，并新增项目 README，包含文件、格式、图片、任务、代码、表格、公式、搜索、导出和快捷键示例。
# M Notes build 4

- 修复应用内选择简体中文后菜单栏、偏好设置和右键菜单仍使用英语的问题；语言变化会立即应用并持久保存。
- 重排偏好设置表单，标签、滑块、数值和开关使用统一列宽与对齐方式。
- 新增“跟随系统”主题并设为默认值，明暗主题现在同时作用于窗口、侧栏、顶栏、设置页和编辑区。
- 字号、行高、正文宽度、行号、拼写检查、智能引号与智能破折号会实时更新编辑器。
- 文件监视开关会立即启动或停止监听；三种图片处理方式会在下一次插入时直接生效。
# M Notes build 5

- 侧边栏文件项支持右键“重命名…”，可直接通过弹窗修改本地文件名，同时无缝同步当前打开文件、阅读位置与最近列表。
- 侧边栏文件列表移除了“已打开”字样角标，点击打开其他文件时不调整已有排列顺序，并支持通过主动拖拽调整文件顺序。
- 偏好设置“通用”页新增“默认文件保存路径”，支持通过访达选取目标目录，新建文档与另存为默认生效。
- “最近使用”新增分类文件夹管理功能，支持新建文件夹、重命名、删除文件夹（删除时文件退回未分类），支持拖拽文件移入分类或通过右键菜单直接归类。
# M Notes build 6

- 工作区 ⌘F 搜索与替换栏重构为现代悬浮卡片样式（毛玻璃材质、圆角阴影、折叠式替换面板、大小写与正则胶囊切换、前后项导航）。
- 偏好设置新增“快捷键”独立设置面板，支持 25 项快捷指令的自定义，支持直接在键盘上按下新组合键录制、单项恢复默认与恢复全部默认，与系统菜单栏动态联动。
- 修复分割线与正文紧挨时正文字体变大的问题：全面移除 Setext 二级标题语法解析，保证 `---` 渲染为水平分割线而不影响上方正文样式。
- 顶栏文档标题支持双击直接弹出重命名窗口修改文件名，并更新悬浮提示。
- 顶栏常用操作区移除专注模式切换按钮，保持顶栏清爽精炼。
- 编辑器增加末尾四分之一页面（25vh）留白机制，无需滚动即可始终在舒适高度编辑，偏好设置支持 0%-60% 自定义，打印和 PDF 导出时自动重置。
- 优化长句换行文本的文字选区居中机制，启用字形精确渲染与折行微距样式，彻底消除第二行高亮框向上拉伸偏移的问题。
# M Notes build 7

- 新增“液态玻璃（浅色）”与“液态玻璃（深色）”主题（毛玻璃材质、微透明卡片、冰晶发光描边、灵动对比度）。
- 偏好设置“主题”页支持在“跟随系统”模式下分别自定义浅色模式与深色模式所匹配的具体主题，系统外观切换时无缝响应。
- 修复台前调度（Stage Manager）软件图标缺失问题：重构生成包含全部 10 档分辨率（16/32/128/256/512 Retina）的完整 `AppIcon.icns`，在 `Info.plist` 中声明 `CFBundleIconFile`，并在应用启动时通过 `NSApplication.shared.applicationIconImage` 主动绑定进程与窗口图标。
# M Notes build 8

- 偏好设置新增“关于”标签页：展示 M Notes 原生高清应用图标、主版本与构建版本号（`v1.0 (Build 4)`）、副标题与版权说明。
- 在线从 GitHub 检查更新：通过 `UpdateChecker` 异步请求 GitHub Releases API（`Is-Lingling/M-Notes`），配合严谨的三段式语义化版本比对（Semantic Versioning）判断新版本状态。
- 查看新版改进说明：提供“查看改进说明”弹窗（`ReleaseNotesSheet`），支持高保真渲染 GitHub Releases 上的 Markdown 正文、发布日期与版本标签。在当前为最新版时亦支持随时查阅当前版本发布说明。
- 一键下载与更新：检测到新版本时展示下载更新卡片与资产大小，支持一键拉起浏览器/系统下载更新包（`M-Notes-v1.0.0-macOS.zip`）或前往 GitHub Release 页面。
- 主菜单栏（App Menu）新增“检查更新…”（`Check for Updates…`）菜单项，支持跟随中英双语动态切换，点击后直达偏好设置关于页并触发检测。
# M Notes build 9

- 移除顶栏多余的侧边栏收起按钮：消除由于 `NavigationSplitView` 原生自带按钮与工具栏按钮并存导致的侧边栏按钮重复问题，并在 `ContentView` 中实现 `columnVisibility` 与 `appState.sidebarVisible` 的无缝双向状态同步。
- 修正新建文件夹按钮位置：从顶栏工具栏中彻底移除，规范放置于可收起侧边栏顶部工具栏中（位于搜索与收起按钮之间），提供明确图标与提示。
- 顶栏双击行内修改文件名：彻底移除模态弹窗对话框，双击顶栏标题后直接原位转为输入框，按回车键（Enter）立即完成保存重命名，按 Esc 或点击外部取消修改，交互更加流畅直观。



