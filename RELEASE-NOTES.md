# M Notes 版本更新说明 (Release Notes)

> **版本**：v1.0.0 (Build 11)  
> **适用平台**：macOS 14.0 (Sonoma) 及更高版本 (Apple Silicon & Intel) / 扩展支持 Windows, Linux  
> **发布日期**：2026年9月  
> **发布包**：  
> - **macOS 磁盘镜像 (推荐)**：`M-Notes-v1.0.0-macOS.dmg` (4.5 MB)  
> - **macOS 绿色便携包**：`M-Notes-v1.0.0-macOS.zip` (4.3 MB)  
> - **Windows 安装包**：`M-Notes-v1.0.0-windows-x64-setup.exe` (3.5 MB) / `.msi` (4.5 MB)  
> - **Linux 安装包**：`m-notes_1.0.0_amd64.deb` (4.7 MB) / `.AppImage` (79.7 MB)  

---

## 目录
1. [最新重点优化 (Build 11: 桌面多平台重构与 DMG 安装包)](#1-最新重点优化-build-11-桌面多平台重构与-dmg-安装包)
2. [界面与交互优化 (Build 10)](#2-界面与交互优化-build-10)
3. [工作区与编辑引擎特性](#3-工作区与编辑引擎特性)
4. [侧边栏与分类文件管理](#4-侧边栏与分类文件管理)
5. [外观主题与液态玻璃设计](#5-外观主题与液态玻璃设计)
6. [快捷键自定义与系统联动](#6-快捷键自定义与系统联动)
7. [关于与 GitHub 检查更新](#7-关于与-github-检查更新)
8. [桌面多平台架构与安装指引](#8-桌面多平台架构与安装指引)
9. [版本历史累计更新汇总](#9-版本历史累计更新汇总)

---

## 1. 最新重点优化 (Build 11: 桌面多平台重构与 DMG 安装包)

### 🚀 macOS DMG 安装包全面支持
- **原生拖拽安装 DMG 镜像**：新增 `dist/M-Notes-v1.0.0-macOS.dmg`，采用 Apple 官方 `UDZO` 压缩技术，容量仅 4.5 MB。双击打开后可直接将 `M Notes` 拖入 `/Applications`（应用程序）文件夹完成安装，享受最地道、最舒适的 Mac 软件安装体验。
- **双发布格式自由选择**：同时提供 `.dmg` 安装镜像与免安装开箱即用的 `.zip` 绿色压缩包，满足不同用户的分发与便携需求。
- **自动化 DMG 构建工具**：引入原生构建脚本 `scripts/build-dmg.sh`，零第三方外部工具依赖，随打随用。

### 🌐 桌面多平台 Monorepo 架构重构与跨端应用扩展
- **`mac/` macOS 原生工程迁移**：原有 macOS 专属工程完整移入独立 `mac/` 目录，并在根目录保留相对软链接向后兼容，确保历史构建与 CI 流程 100% 顺畅执行。
- **`shared/` 统一编辑器内核抽离**：
  - 将 CodeMirror 6 核心、Typora 级隐形 Markdown 折叠扩展、KaTeX 数学公式、Mermaid 矢量图表渲染、Live Tables 交互表格、6 套预编译主题体系与通用图标全面抽离到 `shared/` 模块。
  - 所有平台共享同一套编辑规范与视觉样式，保证多端文档渲染高度一致。
- **`windows/` Windows 桌面端工程**：
  - 基于 WebView2 现代化渲染核心与轻量宿主，支持原生本地文件打开保存、系统托盘与原生窗口装饰，提供 NSIS (`.exe`) 与 MSI (`.msi`) 两种安装规格。
- **`linux/` Linux 桌面端工程**：
  - 基于 WebKitGTK 现代化原生渲染器，遵循 XDG 规范，提供标准 `m-notes.desktop` 桌面启动器与系统主题联动，支持 DEB 软件包与 AppImage 独立运行包。
- **统一构建与 CI/CD 流水线**：提供 `scripts/package-all.sh` 本地打包与 GitHub Actions 自动编译发布流水线。

---

## 2. 界面与交互优化 (Build 10)

### 侧边栏顶栏精简
- **移除冗余按钮**：移除了侧边栏顶部“全文搜索”旁重复的“收起侧边栏”按钮，仅保留“全文搜索”快捷入口与“新建文件夹”按钮，头部视觉更干净协调。
- **顶栏双击就地重命名**：双击顶栏中央文件名时，直接就地转为输入框编辑，按回车键（`Enter`）立即重命名并同步侧边栏与磁盘，按 `Esc` 或失焦即可取消，彻底告别打断思路的弹窗对话框。

### 毫秒级极速主题切换
- **内置主题全量预编译打包**：预打包 6 套官方主题（液态玻璃浅色/深色、Dracula、Solarized、GitHub 浅色/深色）到原生样式资源中，统一使用 CSS 作用域隔离。
- **0ms 零延迟无感切换**：彻底移除以往通过动态修改 `<link href>` 发起本地异步 I/O 的机制，切换主题直接通过根节点数据属性即时生效，无白屏、无闪烁、无重载等待。
- **偏好设置即时联动**：在独立的偏好设置窗口中切换主题，主窗口编辑区与工作区通过通知总线实时同步应用，无需切回主窗口即可秒级见效。

### 工作区与多文档切换性能飞跃
- **消除布局强制重排 (Layout Thrashing)**：优化文档切换时记录阅读位置的逻辑，移除原有的同步 `coordsAtPos` 几何测算，避免切换前强行刷新旧文档 DOM。
- **原子级单次事务提交 (Atomic Transaction)**：将打开文档时的文本替换、语言模式重构、行号显示和光标就位合并为 CodeMirror 6 单次事务（Single Dispatch），减少多次反复重绘。
- **消除初始 redundant IPC 回环**：打开文档时直接复用 Swift 原生已算好的大纲与字数，避免向前端重复派发事件而导致 SwiftUI 视图树反复重绘。

---

## 2. 工作区与编辑引擎特性

- **Typora 风格无感符号折叠**：
  - Markdown 语法标记（`#`、`**`、`*`、`~~`、`` ` ``、`>`、`[text](url)`）在非光标所在行自动零占位隐藏，呈现印刷级排版美感。
  - 光标进入当前行时，所有标记无缝平滑展开，支持完整的原生文字选区与逐字编辑。
- **Live Tables 交互式原生表格**：
  - 渲染为现代化交互表格，支持快捷键向下增行（`⌘⌥↓`）、向右增列（`⌘⌥→`）、删除行（`⌘⌥⌫`）、删除列（`⌘⌥⇧⌫`）。
  - 支持 `Tab` / `⇧Tab` 在单元格间自由跳转流转，表格末端按 Tab 自动增行；点击表格工具条空白处可整表选中，一键 Backspace 删除。
- **Mermaid 图表即时渲染**：
  - 流程图、时序图、类图、甘特图等自动渲染为高清矢量 SVG。
  - 单击图表即可就地展开 Mermaid 源码直接编辑，失焦自动重绘。
- **KaTeX 数学公式渲染**：
  - 支持行内公式 `$E=mc^2$` 与块级公式 `$$...$$` 高精度渲染，支持点击就地查看与修改公式源码。
- **语法高亮代码块**：
  - 集成 highlight.js，支持数十种主流编程语言高亮，配备现代化代码顶栏、语言徽章与一键复制代码按钮。
- **舒适编辑体验与排版保护**：
  - **末尾 25vh 留白**：保证页面底部四分之一空白区域，无需反复滚动页面即可始终在人眼最舒适高度编辑，可在偏好设置中自由调整（0%~60%），打印/导出 PDF 时自动排除。
  - **换行文本选区居中**：精确适配折行微距样式，彻底消除第二行高亮选区向上偏离拉伸的视觉问题。
  - **分割线样式解耦**：彻底消除 `---` 分割线导致上方正文字体异常变大的问题。
  - **现代悬浮查找与替换卡片**：`⌘F` 呼出悬浮毛玻璃卡片，支持匹配计数、大小写敏感、全字匹配、正则模式，支持平滑展开替换行。

---

## 3. 侧边栏与分类文件管理

- **“最近使用”分类文件夹**：
  - 支持新建文件夹、重命名、删除文件夹（删除文件夹时其内文件自动退回“未分类”，不会误删磁盘文件）。
  - 支持拖拽文件自由移入文件夹、拖拽调整文件展示顺序。
  - 支持右键“移动到文件夹”与“未分类（移出文件夹）”。
- **文件管理更纯粹**：
  - 侧边栏文件列表移除了冗余的“已打开”字样角标，以高亮背景与强调图标优雅表示当前激活文档。
  - 点击打开其他文件不打乱原有文件排列顺序。
  - 支持右键文件名进行本地文件的重命名与在访达中显示。
- **自定义默认保存路径**：
  - 偏好设置“通用”面板支持自定义“默认文件保存路径”，可直接点击“选取…”通过 macOS 系统访达对话框设定目标文件夹。
- **全文与文件名即时检索**：
  - 侧边栏常驻“全文搜索”按钮（`⌘⇧F`），支持针对最近使用及工作区所有文档进行全文及标题极速检索。

---

## 4. 外观主题与液态玻璃设计

- **液态玻璃浅色与深色主题 (Liquid Glass)**：
  - 引入 macOS 现代化毛玻璃材质（Translucent Glassmorphism）、冰晶流光描边、轻量浮动阴影与清透层次。
- **跟随系统精细化定制**：
  - 在“跟随系统”外观模式下，用户可分别指定系统为浅色与深色时所激活的具体主题（如白天匹配“液态玻璃（浅色）”，夜晚匹配“Dracula”或“液态玻璃（深色）”）。
- **macOS 台前调度 (Stage Manager) 软件图标修复**：
  - 重构生成包含 10 档分辨率（16/32/128/256/512 Retina）的高清原生 `AppIcon.icns`。
  - 在应用启动时主动将应用图标绑定至当前进程与所有活动窗口，确保在台前调度缩略图、Command-Tab 应用切换器和 Dock 栏中始终清晰显示。

---

## 5. 快捷键自定义与系统联动

- **专属快捷键偏好设置面板**：
  - 偏好设置中包含独立的“快捷键”标签页，支持 25 项高频指令的自定义。
  - 包括加粗、斜体、删除线、行内代码、引用、各级标题（H1~H6）、无序列表、有序列表、任务列表、插入链接、插入图片、插入表格、代码块、水平分割线、查找替换、全文搜索等。
- **键盘一键录制**：
  - 点击对应项后直接在键盘上敲击目标快捷键组合（如 `⌘+⌥+B`），即按即录，直观高效。
  - 支持单项快捷键“恢复默认”，以及底部一键“恢复所有默认”。
  - 快捷键修改后自动与 macOS 系统主菜单栏动态联动同步。

---

## 6. 关于与 GitHub 检查更新

- **专属“关于”偏好设置面板**：
  - 展示 M Notes 高清原生应用图标、主版本与构建版本号（`v1.0 (Build 4)`）、功能介绍与版权信息。
- **GitHub 在线检查更新**：
  - 支持静默自动检查更新（可设置开关）与手动点击“检查更新”。
  - 异步请求 GitHub Releases API (`Is-Lingling/M-Notes`)，使用严谨的三段式语义化版本比对（Semantic Versioning）进行版本评估。
- **查看新版改进说明**：
  - 内置“查看改进说明”功能，通过优雅的 Markdown 模态窗口原生渲染 GitHub Release 发布的详细改进说明日志与版本发布日期。
- **一键下载更新**：
  - 检测到新版时展示直观的下载卡片与安装包文件大小，支持一键调起浏览器下载最新发布包或直接访问发布主页。
- **系统菜单栏快捷入口**：
  - macOS 应用程序主菜单新增“检查更新…”（`Check for Updates…`）入口，支持中英文自适应，点击即可直达。

---

## 8. 多平台架构与安装指引

### 1. macOS (推荐安装方式)
- **方式 A (推荐 DMG 镜像)**：
  - 双击打开 `dist/M-Notes-v1.0.0-macOS.dmg`。
  - 将 `M Notes.app` 拖拽到右侧的 `Applications` 文件夹软链接即可完成安装。
- **方式 B (ZIP 绿色包)**：
  - 解压 `dist/M-Notes-v1.0.0-macOS.zip` 得到 `M Notes.app`。
  - 移动到 `/Applications` 即可使用。

### 2. Windows
- 工程目录：`windows/`
- 构建方式：基于 WebView2 现代化渲染内核与 Tauri 跨平台框架。
  ```bash
  cd windows && npm install && npm run tauri build
  ```
- 输出产物包含 Windows MSI 安装包与轻量独立可执行安装文件（`.exe`）。

### 3. Linux
- 工程目录：`linux/`
- 构建方式：基于 WebKitGTK 与 Tauri 框架。
  ```bash
  cd linux && npm install && npm run tauri build
  ```
- 输出 `.deb`、`.AppImage` 安装包，并附带标准 XDG 桌面启动项 `m-notes.desktop`。

---

## 9. 版本历史累计更新汇总

| 构建版本 | 核心更新内容 |
| :--- | :--- |
| **Build 11** | 桌面多平台 Monorepo 目录重构（`mac/`、`windows/`、`linux/`、`shared/`）；新增 macOS 原生拖拽 DMG 镜像安装包；抽离统一编辑器核心内核；集成 Windows 与 Linux 原生安装打包与发布流水线。 |
| **Build 10** | 侧边栏头部精简（移除多余收起按钮）；顶栏双击就地重命名；预打包 6 套内置主题实现 0ms 极速切换；文档切换原子事务与布局优化；完整打包更新发布。 |
| **Build 9** | 移除顶栏多余侧边栏收起按钮；修正新建文件夹按钮至侧边栏顶部；顶栏双击行内就地修改文件名；双向侧边栏状态同步。 |
| **Build 8** | 偏好设置新增“关于”标签页；支持从 GitHub 检查更新；支持查看新版本改进说明 Markdown 模态弹窗；支持一键下载更新；菜单栏增加“检查更新…”。 |
| **Build 7** | 新增“液态玻璃（浅色）”与“液态玻璃（深色）”主题；支持在跟随系统时分别自定义浅色与深色匹配主题；修复台前调度（Stage Manager）软件图标缺失。 |
| **Build 6** | 工作区 ⌘F 搜索悬浮毛玻璃卡片；偏好设置新增 25 项快捷键全自定义与键盘录制；修复分割线与正文相邻时字体变大问题；末尾 25vh 舒适留白区；换行文本选区居中对齐。 |
| **Build 5** | 侧边栏文件右键重命名；移除“已打开”角标并保持点击不重排顺序；支持拖拽排序；偏好设置新增默认文件保存路径（访达选取）；最近使用分类文件夹管理。 |
| **Build 4** | 完善简体中文与英文多语言即时切换与持久化；重排偏好设置规范表单布局；新增“跟随系统”外观主题；编辑器参数实时响应。 |
| **Build 3** | 优化顶栏长文件名截断与格式按钮；新增矢量 A4 分页 PDF 与单页 PDF 导出；编写《M Notes 使用说明》与英文 Guide。 |
| **Build 1-2** | Typora 风格所见即所得编辑器、CodeMirror 6 引擎、Mermaid 图表、KaTeX 数学公式、Live Tables 交互表格、TXT 纯文本多编码支持。 |

---

## 软件打包与分发产物清单

| 平台 | 安装包名称 | 格式与架构 | 说明 |
| :--- | :--- | :--- | :--- |
| **macOS** | `M-Notes-v1.0.0-macOS.dmg` | DMG 磁盘镜像 (Universal) | 原生拖拽安装至 `/Applications` 文件夹 (4.5 MB) |
| **macOS** | `M-Notes-v1.0.0-macOS.zip` | ZIP 便携压缩包 (Universal) | 解压即用 (4.3 MB) |
| **Windows** | `M-Notes-v1.0.0-windows-x64-setup.exe` | NSIS 安装执行文件 (x64) | Windows 原生安装程序 (3.5 MB) |
| **Windows** | `M-Notes-v1.0.0-windows-x64.msi` | MSI 安装包 (x64) | 企业级 Windows Installer (4.5 MB) |
| **Linux** | `m-notes_1.0.0_amd64.deb` | DEB 软件包 (amd64) | 适用于 Ubuntu / Debian (4.7 MB) |
| **Linux** | `m-notes_1.0.0_amd64.AppImage` | AppImage 独立运行包 (x86_64) | 免安装赋予执行权限直接运行 (79.7 MB) |

